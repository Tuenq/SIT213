language: java
jdk: openjdk11

script:
  - ant runDeploymentTest
 
deploy:
  provider: releases
  api_key: ${GITHUB_OAUTH}
  file: "DUMESTRE-HUGDELARAUZE.tar.gz"
  skip_cleanup: true
  overwrite: true
  on:
    tags: true